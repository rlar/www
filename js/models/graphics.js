define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var graphicsModel = Backbone.Model.extend({
    defaults: {
    },
    initialize: function(){
    }

  });
  return graphicsModel;

});

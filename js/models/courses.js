define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var coursesModel = Backbone.Model.extend({
    defaults: {
    },
    initialize: function(){
    }

  });
  return coursesModel;

});

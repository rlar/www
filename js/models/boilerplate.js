// Filename: models/**typeName**Model
define([
  'Underscore',
  'Backbone'
], function(_, Backbone) {
  var **typeName**Model = Backbone.Model.extend({
    defaults: {
    },
    initialize: function(){
    }
  });
  return **typeName**Model;

});


// example **typeName**: StaticNewsLetterData
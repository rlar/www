define(['app/wedge'], function(wedge) 
{
	var prop = wedge.nRings;

	// do setup work
        return {
            color: "prop: " + prop,
            size: "large",
            addToCart: function() {
            }
        }
    }
);